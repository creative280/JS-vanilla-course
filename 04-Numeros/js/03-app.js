let resultado;

// PI
resultado = Math.PI;

// Redondear
resultado = Math.round(2.5); // 3
resultado = Math.round(2.4); // 2
resultado = Math.round(2.8); // 3
resultado = Math.round(2.3); // 3

// Redondear hacia arriba
resultado = Math.ceil(2.1); // 3

// Redondear hacia abajo
resultado = Math.floor(2.9); // 2

// Raiz cuadrada
resultado = Math.sqrt(144); // 12

// Absoluto
resultado = Math.abs(-500); // 500

// Potencia
resultado = Math.pow(2, 3); // 8

// Minimo
resultado = Math.min(3, 5, 1, 12, -3); // -3

// Maximo
resultado = Math.max(3, 5, 1, 12, -3); // 12

// Aleatorio
resultado = Math.random(); // 0.123456789

// Aleatorio dentro de un rango
resultado = Math.floor(Math.random() * 30); // 0 a 30


console.log(resultado);